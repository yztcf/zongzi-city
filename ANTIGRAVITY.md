# ANTIGRAVITY.md — 粽趣城市專案規則

本檔為「粽趣城市」專案的補充規則，供 Claude Code 在此目錄工作時遵循。

## 專案定位

- **名稱**：粽趣城市（Zongzi City）
- **類型**：端午節主題大富翁（Monopoly-style）網頁桌遊
- **技術**：單檔 HTML + 內嵌 CSS + 原生 JavaScript（無框架、無建置流程）
- **入口**：`index.html`，瀏覽器直接開啟即可遊玩

## 開發約定

- **零依賴優先**：維持單檔可執行、不引入框架與打包工具，除非有明確必要並先確認。
- **遊戲資料與邏輯分離**：棋盤格 `boardLayout`、角色 `characterData`、粽運卡 `fortuneCards` 為資料表，調整玩法數值優先改資料、不動流程函式。
- **繁體中文**：所有面向玩家的文案、commit message 摘要、筆記皆用繁體中文。
- **手動驗證**：每次改動後在瀏覽器（或 preview）實測一輪：選角 → 擲骰 → 買地 → 收租 → 粽運卡 → 轉盤 → 結束回合。

## 已知待精進（詳見 code review 與 README）

- 模態框（modal）以連續 `showModal` 互相覆蓋，缺排隊機制 → 訊息會被瞬間蓋掉。
- 轉盤結果的 `segmentIndex` 與視覺指針未對齊；糖心粽二轉為「隱形計算」非真的轉兩次。
- 破產玩家未真正移出回合輪替，`nextTurn` 仍會輪到他。
- 「經過起點 +200」判斷用 `position < oldPosition`，後退（粽運卡 -2 / 龍船水）也可能誤觸發。
- 雙拼粽「交換人生」技能、轉盤「免費升級地產」尚未實作。

## 版本控制

- 目前僅本地 Git，無遠端。要建立 GitHub 遠端前先確認 repo 名稱與公開/私有。
